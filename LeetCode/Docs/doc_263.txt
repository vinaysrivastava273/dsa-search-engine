. Contains Duplicate III Medium Given an integer array nums and two integers k and t, return true if there are two distinct indices i and j in the array such that abs(nums[i] - nums[j]) <= t and abs(i - j) <= k.
  Array Sliding Window Sorting Bucket Sort Ordered Set
Contains Duplicate
Contains Duplicate II